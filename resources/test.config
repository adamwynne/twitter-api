# Properties for the test config

# TODO: fill these in to do make the tests work

# the consumer oauth stuff for your app
app.consumer.key=
app.consumer.secret=

# this user must be authed on the above app
user.screen.name=
user.access.token=
user.access.token.secret=
